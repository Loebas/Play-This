import { HoofdlettersPipe } from './hoofdletters.pipe';

describe('HoofdlettersPipe', () => {
  it('create an instance', () => {
    const pipe = new HoofdlettersPipe();
    expect(pipe).toBeTruthy();
  });
});
